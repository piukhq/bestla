# Bestla

Account Holder Generator command line tool


## running as script:
- `pipenv install --dev`
- `pipenv run run.py --help`

## running as package
- `pip install -U .`
- `account-holders-generator --help`
